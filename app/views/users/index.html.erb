<div class="team-boxed">
<div class="container">
  <div class="intro">
    <h2 class="text-center">Wonderful and caring pet sitters all around the world</h2>
    <p class="text-center">Because your pets matter, wherever you live</p>
  </div>

  <div class=" people" id="cards">
    <%= link_to new_booking_path do %>
      <div class="col-md-6 col-lg-4 item">
        <div class="box">

          <ul>
            <% @users.each do |user| %>
              <% if user.pet_sitter %>
              <img class="rounded-circle" src="https://source.unsplash.com/random/200x200">
              <li><h2><%= user.name %></h2>
                  <p><%= user.location %></p></li>
            <% end %>

              <% end %>
            <% end %>
          </ul>
            <%#<div class="social"><a href="#"><i class="fa fa-facebook-official"></i></a><a href="#"><i class="fa fa-twitter"></i></a><a href="#"><i class="fa fa-instagram"></i></a></div> %>
          </div>
        </div>
      <div id="map"
          style="width: 100%; height: 600px;"
          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
      </div>
    </div>
  </div>
</div>
